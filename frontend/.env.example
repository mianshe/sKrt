# 本地开发：后端 API 根地址（不要末尾斜杠）。局域网访问时改为本机 IP，例如 http://192.168.1.10:8000
VITE_API_BASE=http://localhost:8000

# 线上（Vercel 同域 /api 代理）：前端请在 Vercel 环境变量中设置
# - VITE_API_BASE=/api
# 并在 Vercel 的 Serverless Function 环境变量中设置
# - BACKEND_URL=https://<你的后端域名>
